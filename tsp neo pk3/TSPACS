#include "zcommon.acs"
#library "tspacs"

script 856 (void)
{
	if(CheckInventory("Scale01") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.1);
		SetActorProperty(0,APROP_ScaleY,0.1);
		TakeInventory("Scale01",1);
		Terminate;
    }
    else if(CheckInventory("Scale02") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.2);
		SetActorProperty(0,APROP_ScaleY,0.2);
		TakeInventory("Scale02",1);
		Terminate;
    }
    else if(CheckInventory("Scale03") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.3);
		SetActorProperty(0,APROP_ScaleY,0.3);
		TakeInventory("Scale03",1);
		Terminate;
    }
	else if(CheckInventory("Scale04") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.4);
		SetActorProperty(0,APROP_ScaleY,0.4);
		TakeInventory("Scale04",1);
		Terminate;
    }
	else if(CheckInventory("Scale05") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.5);
		SetActorProperty(0,APROP_ScaleY,0.5);
		TakeInventory("Scale05",1);
		Terminate;
    }
	else if(CheckInventory("Scale06") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.6);
		SetActorProperty(0,APROP_ScaleY,0.6);
		TakeInventory("Scale06",1);
		Terminate;
    }
	else if(CheckInventory("Scale07") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.7);
		SetActorProperty(0,APROP_ScaleY,0.7);
		TakeInventory("Scale07",1);
		Terminate;
    }
	else if(CheckInventory("Scale08") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.8);
		SetActorProperty(0,APROP_ScaleY,0.8);
		TakeInventory("Scale08",1);
		Terminate;
    }
	else if(CheckInventory("Scale09") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,0.9);
		SetActorProperty(0,APROP_ScaleY,0.9);
		TakeInventory("Scale09",1);
		Terminate;
    }
	else if(CheckInventory("Scale10") > 0)
    {
		SetActorProperty(0,APROP_ScaleX,1.0);
		SetActorProperty(0,APROP_ScaleY,1.0);
		TakeInventory("Scale10",1);
		Terminate;
    }
}

















